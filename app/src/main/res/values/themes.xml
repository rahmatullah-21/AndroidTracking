<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme. Compose drives the real Material 3 colors at runtime; this theme
         just provides a sane splash/window background and an edge-to-edge friendly base. -->
    <style name="Theme.DeviceInsightPro" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- Splash screen shown during cold start (AndroidX core-splashscreen, backported pre-31). -->
    <style name="Theme.DeviceInsightPro.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.DeviceInsightPro</item>
    </style>
</resources>
