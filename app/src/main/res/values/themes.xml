<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme. Compose drives the real Material 3 colors at runtime; this theme
         just provides a sane splash/window background and an edge-to-edge friendly base. -->
    <style name="Theme.DeviceInsightPro" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/white</item>
    </style>
</resources>
